# dingyue 状态

更新时间：2026-09-04

调用 dingyue 后从这里接着做，不要推倒重来。计划按代码现状调整，不按原清单硬走。

## 进行中

- 没绑渠道只出网页：去掉时刻级继承、全部启用渠道回退；新主题/新时段默认空绑定

## 计划

### 这一轮为什么先做这项

订阅页时刻旁已经写「仅网页」，保存时也可以空着渠道。实际投递仍会：
1. 沿用当天其他主题绑过的渠道
2. 再不行就推该用户全部已启用账号
3. 勾选新主题时自动填上通讯录里的账号

今日进度也会按「会外推」来算。这和页面承诺相反，会误推。

ETF 早晨、进度刷新、失败封顶仍然要做，但先把「写了仅网页却还在推」停掉。

### P0

- 没绑渠道只出网页（进行中）
- 订阅页 / 首页今日进度在投递窗口内自动刷新；保存后再拉一次
- 渠道失败设重试上限，最终失败写进通知，并进入「需要你处理」

### P1

- 订阅页「需要你处理」：用户看自己的问题，管理员额外看生成器心跳
- ETF（及别名）只能订傍晚窗口，默认 18:00；「再加时段」和改时刻也要拦
- 当天过近或已过的时刻，保存时明确说会补推、不是准点
- 星期在保存时写死，前后端默认一致（缺字段后端当每天，前端新槽当工作日）

### P2

- 通知记录按时刻聚合（网页 + 各渠道）
- 补扫只覆盖准点后一段窗口（当前 `tick()` 的 `maxLateness` 为 null，等于每分钟扫全天已过时刻）
- 生成 → 入库 → `scheduled:` 投递键的冒烟测试

## 已完成

- dingyue skill 已建；youhua / dianpu / shipin 已改指向

## 已跳过 / 未验证

- （无）

## 待你处理

- （无）
