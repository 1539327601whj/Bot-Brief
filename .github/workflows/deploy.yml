name: Legacy Combined Deploy

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Show migration notice
        run: |
          echo "Combined deploy is disabled."
          echo "Use Deploy Frontend or Deploy Backend workflows instead."
